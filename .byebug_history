exit
@student
    @student.update(student_params)
@student
    @student = Student.find(params[:id])
exit
@student
@student.update(student_params)
@student
    @student = Student.find(params[:id])
@student
student_path(@student)
student_path
